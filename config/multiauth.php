<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default guards to scaffold if command called without args
    |--------------------------------------------------------------------------
    |
    | You can predefine guards here. Artisan command still accepts guard args.
    |
    */
    'guards' => [
        // 'admin' => App\Models\Admin::class,
    ],

    /*
    |--------------------------------------------------------------------------
    | Password broker suffix
    |--------------------------------------------------------------------------
    |
    | Password brokers created for guards will be named as "<guard>s".
    |
    */
    'passwords_suffix' => 's',
];
